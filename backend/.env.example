# Copy to .env (or export in your shell) to enable PostgreSQL persistence.
# Leave DATABASE_URL unset to run with the in-memory store (no DB required).

# Matches docker-compose.yml in the repo root:
DATABASE_URL=postgres://agentops:agentops@localhost:5433/agentops

# Optional: override the API port (default 4000)
# PORT=4000
